# Tests

Project tests live here. The central CI (`ursb-ci-templates`) runs unit tests on every
pipeline. Keep tests fast and deterministic; see
`ursb-engineering-standard/quality/testing-standard.md`.
