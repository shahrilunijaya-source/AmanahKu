# Copy to .env (never commit .env). Real secrets live in the secrets manager / CI variables.
APP_ENV=development
LOG_LEVEL=info
# DATABASE_URL=
# API_BASE_URL=
# Do NOT put Restricted data here. See security/data-classification.md.
